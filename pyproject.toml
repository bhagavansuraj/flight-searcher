[project]
name = "flight-searcher"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.96.0",
    "python-dotenv>=1.2.2",
    "rich>=13.0",
]
